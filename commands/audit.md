---
description: Diff PRD design (expects.*) vs actual code; show missing routes/handlers/hooks/models
argument-hint: "<req-id>"
allowed-tools: ["Bash"]
---

Run `node ${CLAUDE_PLUGIN_ROOT}/bin/codepr.js audit $ARGUMENTS` and report the output verbatim.
