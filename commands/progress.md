---
description: Launch local dashboard at localhost:7878
allowed-tools: ["Bash"]
---

Run `node ${CLAUDE_PLUGIN_ROOT}/bin/codepr.js progress $ARGUMENTS` in the background and report the URL the dashboard is running at. Do not block the session waiting for the server.
