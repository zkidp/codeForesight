---
description: Generate empty placeholder files for missing items in last audit
argument-hint: "<req-id>"
allowed-tools: ["Bash"]
---

Run `node ${CLAUDE_PLUGIN_ROOT}/bin/codepr.js scaffold $ARGUMENTS` and report the output verbatim.
