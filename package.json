{
  "name": "codeforesight",
  "version": "0.5.0-dev",
  "description": "Claude Code plugin: PRD-driven progress board with up-front token/effort estimation, design↔implementation diff, self-contained HTML reports, live SSE dashboard, and proactive skills.",
  "type": "module",
  "bin": {
    "codeforesight": "./bin/codepr.js",
    "cf": "./bin/codepr.js",
    "codepr": "./bin/codepr.js"
  },
  "scripts": {
    "dashboard": "node src/dashboard/server.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage",
    "e2e": "playwright test"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.0",
    "vitest": "^2.1.0"
  },
  "license": "MIT"
}
